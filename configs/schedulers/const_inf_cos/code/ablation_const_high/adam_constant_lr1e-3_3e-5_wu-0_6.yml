{
"optimizer": {
    "type": "Adam",
    "params": {
      "lr": 6.0e-4,
      "betas": [0.9, 0.95],
      "eps": 1.0e-8,
    }
  },
  "lr-decay-style": "constant",
  "warmup": 0,
  "constant_lr": 6.0e-4,
  "constant_iters_percent" : 0.85,
  "cooldown_iters_percent" : 0.6,
  "timescale" : 10,
  "override_lr_scheduler": True, # setting this to True, since we want to train on code for a constant lr
  "use_checkpoint_lr_scheduler": False, # Again, we dont want to use the checkpoint's LR schedular. 
  "tensorboard_dir": "tensorboard/code/inf_cos/high/ablation_6e_4",
  "log_dir": "logs/code/inf_cos/high/ablation_6e_4",
}